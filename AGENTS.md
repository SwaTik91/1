# AGENTS.md

## Cursor Cloud specific instructions

This repository (`SwaTik91/1`) is an empty skeleton project. As of the last setup review:

- **No source code, applications, or services exist.** The repo contains only `.gitattributes`, `.gitignore` (C/C++ artifact patterns), and a GPLv3 `LICENSE`.
- **No dependencies to install**, no build system, no package manager config, and no test framework.
- **No lint, test, or build commands** are available.
- The `.gitignore` hints at a potential C/C++ project, but no source files have been committed.

If code is added in the future, update this file with setup, build, test, and run instructions.
